spring:
  datasource:
    url: jdbc:h2:mem:forcatotal;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true

management:
  endpoint:
    health:
      show-details: always
